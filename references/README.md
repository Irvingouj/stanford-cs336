# Reference Implementations — 做完作业再看 ⚠️

These are tutorial implementations from the lectures. They overlap with
the assignments. **Don't read them until you've finished the corresponding
`src/cs336/` module yourself.**

| file | overlaps with | read after |
|------|---------------|------------|
| `lecture_01_tokenization.py` | A1: `src/cs336/tokenizer/bpe.py` | BPE encode/decode/train done |
| `lecture_03_architectures.py` | A1: `src/cs336/architecture/transformer.py` | Transformer block + LM done |
| `lecture_06_kernels_triton.py` | A2: `src/cs336/systems/kernels.py` | FlashAttention done |
| `lecture_15_16_training.py` | A5: DPO, GRPO, reward model | Alignment done |

These are simplified teaching versions — the actual assignments require
more rigorous implementations that pass specific test suites. So even
after reading them, you still have work to do.
