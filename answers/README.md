# Answers

Editable assignment copies.

Run each assignment from its own folder:

```bash
cd answers/assignment1-basics
uv run pytest
```

These folders are copied from `reference/assignment*` so edits here do not change the installed tutorials.
